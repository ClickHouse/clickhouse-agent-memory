{
  "ran_at": "2026-04-22T07:34:59Z",
  "ch_version": "26.3.9.8",
  "ch_http": "http://localhost:18123",
  "database": "enterprise_memory",
  "canonical_service": "svc-orders",
  "canonical_user": "u-maruthi",
  "canonical_domain": "observability",
  "canonical_incident_id": "04e3c725-1ff3-4b1c-b633-33dc3dc4532e",
  "iterations": 25,
  "warmup": 3,
  "sql_source": "cookbooks/mcp_server/queries.py (live MCP templates, same as production)",
  "duration_s": 9.31,
  "scenarios": [
    {
      "demo": 1,
      "demo_name": "HOT alone \u2014 what just happened?",
      "tier": "HOT",
      "tool": "search_events",
      "user_question": "\"What is happening on svc-orders right now?\"",
      "params": {
        "service": "svc-orders",
        "minutes": "60",
        "limit": "20"
      },
      "feature": "Memory engine \u00b7 WHERE + ORDER BY ts DESC \u00b7 time window + severity filter",
      "proves": "Single-digit ms signals from a SQL surface. Same engine as warm + graph.",
      "caveat": "Redis is faster at this in isolation (sub-ms). CH wins only when the hot tier needs to compose with the others.",
      "source": "cookbooks/mcp_server/queries.py :: HOT_SCAN_SQL['observability']",
      "sql_rendered": "-- HOT tier: live event stream for the observability domain.\n        -- obs_events_stream uses ENGINE = Memory, so this read is sub-5ms\n        -- and the data is volatile (cleared on container restart).\n        SELECT event_id, ts, service, host, level, message,\n               latency_ms, error_code, trace_id\n        FROM enterprise_memory.obs_events_stream\n        -- Narrow by service name if the caller gave one; otherwise match all.\n        WHERE (length({service:String}) = 0 OR service = {service:String})\n          -- Only errors + criticals; INFO / WARN are not useful for triage.\n          AND level IN ('ERROR', 'CRITICAL')\n          -- Rolling window: last N minutes of the live stream.\n          AND ts >= now() - INTERVAL {minutes:UInt32} MINUTE\n        ORDER BY ts DESC\n        LIMIT {limit:UInt32}",
      "iterations": 25,
      "read_rows_p50": 200,
      "read_bytes_p50": 28193,
      "query_duration_ms_p50": 1.0,
      "query_duration_ms_p95": 2.0,
      "result_rows_p50": 0
    },
    {
      "demo": 2,
      "demo_name": "WARM alone \u2014 have we seen this before?",
      "tier": "WARM",
      "tool": "semantic_search",
      "user_question": "\"Find past incidents that look like a database connection timeout on svc-orders.\"",
      "params": {
        "k": "5"
      },
      "feature": "MergeTree + HNSW vector_similarity('hnsw','cosineDistance',768) inside the table",
      "proves": "Filter-first retrieval. HNSW rank on the surviving set.",
      "caveat": "pgvector / Qdrant do this too. CH wins when filter volumes + data are heavy.",
      "source": "cookbooks/mcp_server/queries.py :: WARM_VECTOR_SQL['observability']",
      "sql_rendered": "-- WARM tier: SEMANTIC SEARCH over past observability incidents.\n        -- `embedding` is Array(Float32). cosineDistance returns 0 for an\n        -- identical vector and 2 for the opposite direction, so LOWER is\n        -- more similar. The HNSW index makes the ORDER BY LIMIT k fast.\n        SELECT incident_id, ts, title, affected_services,\n               root_cause, resolution,\n               toString(severity) AS severity, duration_min,\n               -- Rounded copy for display; the un-rounded form drives the sort.\n               round(cosineDistance(embedding, [\u2026768-d query vector\u2026]), 4) AS similarity_distance\n        FROM enterprise_memory.obs_historical_incidents\n        -- ORDER BY uses the un-rounded distance so the HNSW index kicks in.\n        ORDER BY cosineDistance(embedding, [\u2026768-d query vector\u2026]) ASC\n        LIMIT {k:UInt32}",
      "iterations": 25,
      "read_rows_p50": 14,
      "read_bytes_p50": 26362,
      "query_duration_ms_p50": 3.0,
      "query_duration_ms_p95": 5.0,
      "result_rows_p50": 5
    },
    {
      "demo": 3,
      "demo_name": "GRAPH alone \u2014 what breaks if this fails?",
      "tier": "GRAPH",
      "tool": "find_related_entities",
      "user_question": "\"What services depend on svc-orders?\"",
      "params": {
        "entity": "svc-orders",
        "max_hops": "2"
      },
      "feature": "Two-hop upstream dependency walk \u00b7 SQL JOIN + UNION ALL",
      "proves": "Graph walks don't need a separate graph DB for 2-hop blast radius.",
      "caveat": "Neo4j / Memgraph still win at hard graph algorithms.",
      "source": "cookbooks/mcp_server/queries.py :: GRAPH_TRAVERSE_SQL['observability']",
      "sql_rendered": "-- GRAPH tier: multi-hop dependency traversal for observability.\n        -- Question: \"who depends on <service>?\" (upstream blast radius).\n        --\n        -- Hop 1: direct dependents of the target service.\n        SELECT d.from_service AS related, s.criticality, s.team,\n               d.dep_type AS edge_type, d.latency_p99, 1 AS hops\n        FROM enterprise_memory.obs_dependencies d\n        JOIN enterprise_memory.obs_services s ON s.service_id = d.from_service\n        WHERE d.to_service = {entity:String}\n\n        UNION ALL\n\n        -- Hop 2: services that depend on services that depend on the target.\n        -- Only emitted when max_hops >= 2.\n        SELECT d2.from_service, s2.criticality, s2.team,\n               d2.dep_type, d2.latency_p99, 2 AS hops\n        FROM enterprise_memory.obs_dependencies d2\n        JOIN enterprise_memory.obs_services s2 ON s2.service_id = d2.from_service\n        WHERE d2.to_service IN (\n            SELECT from_service FROM enterprise_memory.obs_dependencies\n            WHERE to_service = {entity:String}\n        )\n        AND {max_hops:UInt32} >= 2",
      "iterations": 25,
      "read_rows_p50": 53,
      "read_bytes_p50": 1285,
      "query_duration_ms_p50": 3.0,
      "query_duration_ms_p95": 5.0,
      "result_rows_p50": 2
    },
    {
      "demo": 4,
      "demo_name": "MIXED \u2014 walk me through it",
      "tier": "ALL",
      "tool": "search_events",
      "step": "1/4",
      "sub_intent": "live errors on the failing service",
      "user_question": "\"svc-orders is failing, walk me through it.\"",
      "params": {
        "service": "svc-orders",
        "minutes": "60",
        "limit": "10"
      },
      "source": "HOT_SCAN_SQL['observability']",
      "sql_rendered": "-- HOT tier: live event stream for the observability domain.\n        -- obs_events_stream uses ENGINE = Memory, so this read is sub-5ms\n        -- and the data is volatile (cleared on container restart).\n        SELECT event_id, ts, service, host, level, message,\n               latency_ms, error_code, trace_id\n        FROM enterprise_memory.obs_events_stream\n        -- Narrow by service name if the caller gave one; otherwise match all.\n        WHERE (length({service:String}) = 0 OR service = {service:String})\n          -- Only errors + criticals; INFO / WARN are not useful for triage.\n          AND level IN ('ERROR', 'CRITICAL')\n          -- Rolling window: last N minutes of the live stream.\n          AND ts >= now() - INTERVAL {minutes:UInt32} MINUTE\n        ORDER BY ts DESC\n        LIMIT {limit:UInt32}",
      "iterations": 25,
      "read_rows_p50": 200,
      "read_bytes_p50": 28193,
      "query_duration_ms_p50": 1.0,
      "query_duration_ms_p95": 2.0,
      "result_rows_p50": 0
    },
    {
      "demo": 4,
      "demo_name": "MIXED \u2014 walk me through it",
      "tier": "ALL",
      "tool": "semantic_search",
      "step": "2/4",
      "sub_intent": "find similar past incident",
      "params": {
        "k": "3"
      },
      "source": "WARM_VECTOR_SQL['observability']",
      "sql_rendered": "-- WARM tier: SEMANTIC SEARCH over past observability incidents.\n        -- `embedding` is Array(Float32). cosineDistance returns 0 for an\n        -- identical vector and 2 for the opposite direction, so LOWER is\n        -- more similar. The HNSW index makes the ORDER BY LIMIT k fast.\n        SELECT incident_id, ts, title, affected_services,\n               root_cause, resolution,\n               toString(severity) AS severity, duration_min,\n               -- Rounded copy for display; the un-rounded form drives the sort.\n               round(cosineDistance(embedding, [\u2026768-d query vector\u2026]), 4) AS similarity_distance\n        FROM enterprise_memory.obs_historical_incidents\n        -- ORDER BY uses the un-rounded distance so the HNSW index kicks in.\n        ORDER BY cosineDistance(embedding, [\u2026768-d query vector\u2026]) ASC\n        LIMIT {k:UInt32}",
      "iterations": 25,
      "read_rows_p50": 13,
      "read_bytes_p50": 26080,
      "query_duration_ms_p50": 3.0,
      "query_duration_ms_p95": 7.0,
      "result_rows_p50": 3
    },
    {
      "demo": 4,
      "demo_name": "MIXED \u2014 walk me through it",
      "tier": "ALL",
      "tool": "get_record",
      "step": "3/4",
      "sub_intent": "hydrate top match (runbook + resolution)",
      "params": {
        "identifier": "04e3c725-1ff3-4b1c-b633-33dc3dc4532e"
      },
      "source": "WARM_LOOKUP_SQL[('observability','runbook')]",
      "sql_rendered": "-- WARM tier: FETCH A RUNBOOK BY ID.\n        -- Deterministic lookup: no vector math, just the full\n        -- historical incident row so the agent can quote the resolution.\n        SELECT incident_id, title, root_cause, resolution,\n               duration_min, toString(severity) AS severity, affected_services\n        FROM enterprise_memory.obs_historical_incidents\n        WHERE incident_id = {identifier:String}",
      "iterations": 25,
      "read_rows_p50": 1,
      "read_bytes_p50": 283,
      "query_duration_ms_p50": 1.0,
      "query_duration_ms_p95": 1.0,
      "result_rows_p50": 1
    },
    {
      "demo": 4,
      "demo_name": "MIXED \u2014 walk me through it",
      "tier": "ALL",
      "tool": "find_related_entities",
      "step": "4/4",
      "sub_intent": "2-hop blast radius downstream",
      "params": {
        "entity": "svc-orders",
        "max_hops": "2"
      },
      "source": "GRAPH_TRAVERSE_SQL['observability']",
      "sql_rendered": "-- GRAPH tier: multi-hop dependency traversal for observability.\n        -- Question: \"who depends on <service>?\" (upstream blast radius).\n        --\n        -- Hop 1: direct dependents of the target service.\n        SELECT d.from_service AS related, s.criticality, s.team,\n               d.dep_type AS edge_type, d.latency_p99, 1 AS hops\n        FROM enterprise_memory.obs_dependencies d\n        JOIN enterprise_memory.obs_services s ON s.service_id = d.from_service\n        WHERE d.to_service = {entity:String}\n\n        UNION ALL\n\n        -- Hop 2: services that depend on services that depend on the target.\n        -- Only emitted when max_hops >= 2.\n        SELECT d2.from_service, s2.criticality, s2.team,\n               d2.dep_type, d2.latency_p99, 2 AS hops\n        FROM enterprise_memory.obs_dependencies d2\n        JOIN enterprise_memory.obs_services s2 ON s2.service_id = d2.from_service\n        WHERE d2.to_service IN (\n            SELECT from_service FROM enterprise_memory.obs_dependencies\n            WHERE to_service = {entity:String}\n        )\n        AND {max_hops:UInt32} >= 2",
      "iterations": 25,
      "read_rows_p50": 53,
      "read_bytes_p50": 1285,
      "query_duration_ms_p50": 3.0,
      "query_duration_ms_p95": 4.0,
      "result_rows_p50": 2
    }
  ]
}
